all:
    children:
        web_load_balancers:
            hosts:
                lbweb: 
                    ansible_connection: docker
        backservers:
            hosts:
                nodo_a: 
                    ansible_connection: docker
                nodo_b: 
                    ansible_connection: docker
        frontservers:
            hosts:
                nodo_a:
                nodo_b:
        databases:
            hosts:
                postgres: 
                    ansible_connection: docker
            vars:
                database_su_name: postgres
                database_su_password: Local123
                # crear usuario con otro nombre via socket
                # postgresql_users:
                #     - name: sgh2020
                #       password: sgh2020
                #       login_unix_socket: /var/postgres/pg.socket
vars:
    ansible_ssh_pipelining: true
