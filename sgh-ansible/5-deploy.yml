---
  - hosts: localhost
    tasks:
      - name : migrate
        shell: ./migrate.sh
  - hosts: frontservers
    roles:
      - role: sgh-front
        become: yes
  - hosts: backservers
    roles:
      - role: sgh-back
        become: yes