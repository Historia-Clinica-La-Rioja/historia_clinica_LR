
import { SupportedLanguages } from '../../libs/sgx/api/model';

import englishMessages from './en';
import spanishMessages from './es';

const messages: SupportedLanguages = {
    en: englishMessages,
    es: spanishMessages,
}

export default messages;