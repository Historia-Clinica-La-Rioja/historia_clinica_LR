import { FullHouseAddressPipe } from './fullHouseAddress.pipe';

describe('FullHouseAddressPipe', () => {
  it('create an instance', () => {
    const pipe = new FullHouseAddressPipe();
    expect(pipe).toBeTruthy();
  });
});
