import { ViewHourMinutePipe } from './view-hour-minute.pipe';

describe('ViewHourMinutePipe', () => {
  it('create an instance', () => {
    const pipe = new ViewHourMinutePipe();
    expect(pipe).toBeTruthy();
  });
});
