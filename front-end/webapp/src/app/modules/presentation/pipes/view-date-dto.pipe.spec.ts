import { ViewDateDtoPipe } from './view-date-dto.pipe';

describe('ViewDateDtoPipe', () => {
	it('create an instance', () => {
		const pipe = new ViewDateDtoPipe();
		expect(pipe).toBeTruthy();
	});
});
